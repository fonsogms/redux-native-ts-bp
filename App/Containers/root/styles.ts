import { StyleSheet } from 'react-native';

export const MainView = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'red',
    justifyContent: 'center',
    alignItems: 'center',
    alignContent: 'center',
  },
});

export const ButtonView = StyleSheet.create({
  container: {
    backgroundColor: 'blue',
  },
});

export const PressHereText = StyleSheet.create({
  container: {
    color: 'white',
  },
});
